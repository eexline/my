# Многоязычность (i18n)

Сайт поддерживает многоязычность с автоматическим определением языка пользователя и возможностью ручного переключения.

## Поддерживаемые языки

- 🇷🇺 Русский (ru) - по умолчанию
- 🇬🇧 English (en)

## Как добавить новый язык

1. Создайте файл `lib/translations/[locale].ts` (например, `de.ts` для немецкого)
2. Скопируйте структуру из `ru.ts` или `en.ts`
3. Добавьте переводы для всех ключей
4. Добавьте язык в `lib/i18n.ts`:
   ```typescript
   export type Locale = 'ru' | 'en' | 'de' // добавьте новый язык
   export const locales: Locale[] = ['ru', 'en', 'de'] // добавьте в массив
   ```
5. Добавьте название и флаг в `components/LanguageSwitcher.tsx`:
   ```typescript
   const languageNames: Record<Locale, string> = {
     ru: 'Русский',
     en: 'English',
     de: 'Deutsch', // добавьте
   }
   ```

## Использование переводов

В клиентских компонентах:
```tsx
'use client'
import { useTranslations } from '@/hooks/useTranslations'

export function MyComponent() {
  const t = useTranslations()
  
  return <h1>{t.home.hero.title}</h1>
}
```

## Автоопределение языка

Язык определяется автоматически:
1. Из cookies (если пользователь уже выбирал язык)
2. Из заголовка `Accept-Language` браузера
3. По умолчанию: русский

## Сохранение выбора

Выбранный язык сохраняется в cookies и будет использоваться при следующих посещениях.

